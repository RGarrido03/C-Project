group Class;

createPenClass(className, classProps) ::= <<
class <className>(Pen):
    def __init__(self) -> None:
        super().__init__()
        <classProps; separator="\n">
>>

classProp(prop, value) ::= "self.<prop> = <value>"

object(variable, penType) ::= "<variable> = <penType>()"
